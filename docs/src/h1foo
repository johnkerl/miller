#!/bin/bash

awk '{
  if ($0 ~ /.*<h1>.*/) {
    sub(/<h1>/, "");
    sub(/<\/h1>/, "");
    print;
    print "----------------------------------------------------------------";
  } else {
    print $0
  }
}' "$@"
