#!/bin/bash

awk '{
  if ($0 ~ /.*<h3>.*/) {
    sub(/<h3>/, "");
    sub(/<\/h3>/, "");
    print;
    print "................................................................";
  } else {
    print $0
  }
}' "$@"
