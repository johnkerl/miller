#!/bin/bash

path_to_mlr="./mlr"
indir="./regtest/input"

go build mlr.go
if [ $? -ne 0 ]; then
  exit 1
fi

# ----------------------------------------------------------------
run_mlr() {
  echo
  echo ---------------------------------------------------------------- "$@"
  echo $path_to_mlr "$@"
  echo
  $path_to_mlr "$@"
}

# ----------------------------------------------------------------
mlr_expect_fail() {
  echo
  echo $path_to_mlr "$@"
  echo
  $path_to_mlr "$@"
  status=$?
  if [ $status -eq 0 ]; then
    echo "failed to fail"
  fi
}

# ----------------------------------------------------------------
run_cat() {
  echo
  echo cat "$@"
  echo
  cat "$@"
}

# ----------------------------------------------------------------
announce() {
  echo
  echo "================================================================"
  echo
  echo "$@"
}

# ----------------------------------------------------------------
mention() {
  echo
  echo ---------------------------------------------------------------- "$@"
}

# ================================================================

mlr --from regtest/input/reorder.dkvp reorder -f x,y -b u
mlr --from regtest/input/reorder.dkvp reorder -f u,v -b x
mlr --from regtest/input/reorder.dkvp reorder -f z,w,s -b v
mlr --from regtest/input/reorder.dkvp reorder -f u,v -b nonesuch
mlr --from regtest/input/reorder.dkvp reorder -f u,nonesuch -b x

mlr --from regtest/input/reorder.dkvp reorder -f x,y -a u
mlr --from regtest/input/reorder.dkvp reorder -f u,v -a x
mlr --from regtest/input/reorder.dkvp reorder -f z,w,s -a v
mlr --from regtest/input/reorder.dkvp reorder -f u,v -a nonesuch
mlr --from regtest/input/reorder.dkvp reorder -f u,nonesuch -a x

# ================================================================
