#!/bin/bash

verbose=""
if [ "$1" = "-v" ]; then
  set -x
  verbose="-v"
fi

set -euo pipefail

./build-go

echo
go test ./...

echo
mlr regtest reg-test/cases reg-test/cases-pending $verbose

echo
./reg-test/run -n $verbose
