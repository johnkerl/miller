mlr: cannot parse DSL expression.
Parse error on token "" at line 9 columnn 1.
Please check for missing semicolon.
Expected one of:
  ; } > >> | ( ? || ^^ && ?? ??? =~ !=~ == != >= < <= ^ & << >>> + - .+ .-
  . * / // % .* ./ .// ** [ [[ [[[

