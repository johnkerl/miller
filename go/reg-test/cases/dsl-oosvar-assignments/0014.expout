DSL EXPRESSION:
$* = @v[1]


AST:
* statement block
    * assignment "="
        * full record "$*"
        * array or map index access "[]"
            * direct oosvar value "v"
            * int literal "1"

