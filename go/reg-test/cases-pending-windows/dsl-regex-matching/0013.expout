DSL EXPRESSION:
$x =~ "^a.*"."d$"i

AST:
* statement block
    * bare boolean
        * operator "=~"
            * direct field value "x"
            * operator "."
                * string literal "^a.*"
                * case-insensitive regular expression ""d$"i"

