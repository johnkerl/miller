DSL EXPRESSION:
for (k in $*) {2}


AST:
* statement block
    * single-variable for-loop "for"
        * local variable "k"
        * full record "$*"
        * statement block
            * bare boolean
                * int literal "2"

