#!/bin/bash

awk '{
  if ($0 ~ /.*<h2>.*/) {
    sub(/<h2>/, "");
    sub(/<\/h2>/, "");
    print;
    print "^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^";
  } else {
    print $0
  }
}' "$@"
