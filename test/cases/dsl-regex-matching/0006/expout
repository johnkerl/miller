DSL EXPRESSION:
$x =~ "^a.*"."d$"


AST:
* statement block
    * bare boolean
        * operator "=~"
            * direct field value "x"
            * dot operator "."
                * string literal "^a.*"
                * string literal "d$"

x=abcd,y=ghi
